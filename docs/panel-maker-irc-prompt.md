Rebuild the IRC Minimal panel maker to the same standard as the Retro Messenger one.

**Context.** Repo is the connected `Claude Senergy Group` folder. We just rebuilt `LandingPage/public/panel-makers/retro-messenger.html` (Win98/AOL Twitch panel maker) through an independent-review loop and shipped it at 8.2/8.1. Do the same for the IRC Minimal family. Practical notes: `device_bash` may fail to mount in this repo — use the list/stage/commit file tools instead. Sandbox: Google Fonts and github.com are blocked; npm `@fontsource/*` and raw.githubusercontent.com work; Chromium + Playwright, PIL/FreeType and fontTools are available (no brotli, so use .woff not .woff2).

**Read first, before writing code.**
1. `LandingPage/public/panel-makers/retro-messenger.html` — the base. Reuse its engine as-is: 1-bit glyph-atlas text (rasterise at 1x, blit at exactly 2x on even coords — this is what fixed legibility, do not go back to canvas vector text or @font-face pixel fonts), preset/layout system, zip writer + README.txt, localStorage, include-in-zip checkboxes, suggested Twitch description + copy button, Preview all, true 320px-on-#18181b preview, and the `window.__panelMaker.render(key)` hook used for captures.
2. `LandingPage/docs/panel-maker-review-2026-09-09.md` — round log, what failed and why, the still-open list, and the reuse notes for IRC.
3. `LandingPage/docs/panel-maker-make_atlas.py` — atlas generator (any TTF → JSON glyph atlas).
4. `LandingPage/README_PANEL_GUIDE.md` — house spec: single self-contained HTML, canvas only, no libraries, 640x200 export (2x of Twitch's 320px column), Twitch upload hint, UTM-tagged Etsy CTA.
5. IRC design source: `widgets/sku-06-irc-minimal/` (css.txt, html.txt, custom_fields.txt, captures/) and `widgets/sku-irc-06-panels/panels.json` (tokens: bg #16181D, bar #2A2A2A, accent #90A4AE, text #D4D4D4, title #6BC5FF, ok #6BFF6B; IBM Plex Mono). The v1 maker is `LandingPage/public/panel-makers/irc-minimal.html` — back it up to `LandingPage/panel-makers-backup/irc-minimal.v1-<date>.html` before replacing it, and keep its CTA UTM `utm_source=panel-maker&utm_medium=web&utm_campaign=irc-minimal`. The `/panel-maker/irc-minimal` route is already whitelisted in `server.js`; don't edit it.

**Build.** Keep the same 10 presets (About, Schedule, Rules, Socials, Discord, Subscribe, Support, Commands, Specs, Custom) and the one-link-per-image rule (Socials = one row per platform, each with its own handle). Swap everything family-specific:
- Chrome: a classic IRC client / terminal window instead of Win98 — `#channel` topic line, `[hh:mm]` timestamps, `<nick>` brackets, `*** joins`/`/topic`/`/mode` server lines, a nick list or status line, block cursor. Study the widget captures so it reads as the same product.
- Faces: a monospace through the same atlas pass (IBM Plex Mono from `@fontsource/ibm-plex-mono`, or DejaVu Sans Mono which is installed). Pick sizes that survive 320px — likely 13–14 body, 16 bold titles — and verify at true size.
- Themes: 5, starting from the widget's defaults (e.g. IRC dark, green phosphor, amber, mIRC light, solarized). Run a ≥5:1 contrast guard on every theme.
- Icons and gags: 16x16 pixel icons in the IRC palette; IRC-flavoured copy (`/join #discord`, `* streamer sets mode +o`, `--- Day changed`, `!commands`, uptime, `/whois`).
- While you're in there, address the retro maker's still-open items: fill the empty right half of list panels, a larger body strike, bigger social marks, export buttons at the end of the controls.

**Review loop.** After each build, spawn a fresh independent reviewer (a new general-purpose agent with no memory of the build) with this persona: a graphic designer who is also a ~150-viewer Twitch streamer and buys panel packs. Give it the HTML plus Playwright captures: desktop and mobile UI, every preset at 2x and at true 320px on #18181b, an avatar-upload + 21-character-name test, and two theme variants. It grades A Authenticity, B Legibility at 320px on dark, C Hierarchy, D Craft (pixel grid, colour count in text regions, alignment), E Usefulness to a streamer, F Maker UX — each /10 — then OVERALL x.x, TOP 5 CHANGES, NITS, VERDICT. Target 8.5. Maximum 3 revisions after the first review; fix the top changes each round; then accept the best build. Report every score honestly — never inflate — and disclose any post-review tweaks that weren't re-reviewed.

**Deliver.** Save into the repo: the new `irc-minimal.html`, the v1 backup, `LandingPage/docs/panel-maker-irc-review-<date>.md` (round table + what's in v2 + still open, like the retro log), a contact-sheet PNG, and the atlas script if it changed. Send me the HTML and the review log, and update project memory (`panel-maker-v2.md`) with what changed and what's still open. No git commits unless I ask.
